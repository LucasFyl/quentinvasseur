$(document).ready(function(){
	// le loader :

	

	// Dropdown du menu
	// $('body').on('click', 'a.dropdown', function(e){
	// 	e.preventDefault();
	// 	var menu = $(this).siblings('.dropdown-menu')
	// 	if ($(this).hasClass('active')) {
	// 		$(this).removeClass('active');
	// 		TweenMax.to(menu, 0.45, {height:'-=168px',display:'none',ease:Power3.easeOut});
	// 	} else {
	// 		$(this).addClass('active');
	// 		TweenMax.to(menu, 0.45, {height:'+=168px',display:'block',ease:Power3.easeOut});
	// 	}
	// });
	// $('body').on('click', 'a.dropdown-sneaker', function(e){
	// 	e.preventDefault();
	// 	var menu = $(this).siblings('.dropdown-menu')
	// 	if ($(this).hasClass('active')) {
	// 		$(this).removeClass('active');
	// 		TweenMax.to(menu, 0.45, {height:'-=340px',display:'none',ease:Power3.easeOut});
	// 	} else {
	// 		$(this).addClass('active');
	// 		TweenMax.to(menu, 0.45, {height:'+=340px',display:'block',ease:Power3.easeOut});
	// 	}
	// });
	// $('body').on('click', 'li.direct', function(e){
	// 	TweenMax.to('body>*', 0.25,{opacity:0,ease:Linear.easeNone})
	// });
});



